import { GamecodigoPipe } from './gamecodigo.pipe';

describe('GamecodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new GamecodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
